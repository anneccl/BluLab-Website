import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Évènements"
};

const DropDetails = () => {
    return(
            <main>
                <h1> Drop[id] </h1>
            </main>
            
    )
}

export default DropDetails
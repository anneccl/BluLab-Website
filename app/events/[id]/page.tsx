import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Évènements"
};

const EventDetails = () => {
    return(
            <main>
                <h1> Events[id] </h1>
            </main>
            
    )
}

export default EventDetails